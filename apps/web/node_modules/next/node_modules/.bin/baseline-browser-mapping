#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../baseline-browser-mapping/dist/cli.cjs" "$@"
else
  exec node  "$basedir/../../../baseline-browser-mapping/dist/cli.cjs" "$@"
fi
# cmd-shim-target=D:/WorkSpace/new-project/node_modules/.pnpm/next@16.2.6_@babel+core@7.2_7d7ddfdd25bda7360fc181e5027d31b3/node_modules/baseline-browser-mapping/dist/cli.cjs
